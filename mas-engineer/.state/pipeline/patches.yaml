stage: 3
agent: im-designer
timestamp: '2026-07-24T07:22:00+00:00'
input_file: .state/pipeline/ranked_findings.yaml
signal: DONE
request_id: "20260724_34"
from: sub_mas-im-designer
to: sub_mas-general-improver
status: success
data:
  patches:
    # ─────────────────────────────────────────────────────────────
    # F-1727: sub_mas-python-repair.yaml — NN1 split (6 roles)
    # Roles: analyze, validate, repair, read, write
    # Tasks: COMPILE, ANALYZE, FIX, VALIDATE
    # ─────────────────────────────────────────────────────────────
    - type: create_orchestrator
      file: recipe/sub/sub_mas-python-repair-director.yaml
      content: |
        name: MAS Python Repair Director
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 🧬 SUB-MAS-PYTHON-REPAIR-DIRECTOR — Python Repair Orchestrator
        description: 'v1.0.0 | MAS-internal: Orchestrates Python repair pipeline by delegating to specialized sub-agents'
        instructions: |
          # PYTHON-REPAIR-DIRECTOR v1.0.0
          # Orchestrator — delegates to specialized sub-agents

          ## ⛔ ONLY orchestration — NO direct Python repair
          → Delegate to specialized sub-agents based on task

          ## Delegation Map
          - analyze/compile/scan → sub_mas-python-analyzer
          - fix/repair/patch → sub_mas-python-fixer
          - validate/verify → sub_mas-python-validator

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am python-repair-director (v1.0.0) | MODE-CHECK: mas-engineer | 🧬 PYTHON-REPAIR-DIRECTOR (v1.0.0)
          ⛔ ONLY orchestration — NO direct Python repair
          → Delegate to specialized sub-agents based on task
        sub_recipes:
          - name: sub_mas-python-analyzer
            path: ./sub_mas-python-analyzer.yaml
            description: Python code analysis and compilation checks
          - name: sub_mas-python-fixer
            path: ./sub_mas-python-fixer.yaml
            description: Python code repair and patching
          - name: sub_mas-python-validator
            path: ./sub_mas-python-validator.yaml
            description: Python code validation and verification
        settings:
          timeout: 600
          max_steps: 100
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: multi_role_agent with 6 distinct roles (analyze, validate, repair, read, write).
        Split into orchestrator + 3 specialized sub-agents (analyzer, fixer, validator).
        Each sub-agent has single responsibility. Original archived to legacy.
        Goose-expert verdict: NOT REQUIRED — NN1 type not in mandatory consultation list.
      priority: 0.58
      current_chars: 1450
      target_chars: 1850

    - type: create_sub_agent
      file: recipe/sub/sub_mas-python-analyzer.yaml
      content: |
        name: MAS Python Analyzer
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 🔬 SUB-MAS-PYTHON-ANALYZER — Python Code Analysis
        description: 'v1.0.0 | MAS-internal: Python code syntax check and structure analysis'
        instructions: |
          # PYTHON-ANALYZER v1.0.0
          # Analyze Python code — syntax and structure

          ## ⛔ ONLY analysis — NO fixing or validation
          → Analyze Python files and return results

          ## Capabilities
          - COMPILE: python3 -c "compile(...)" syntax check
          - ANALYZE: python3 ast parsing for functions, classes, imports
          - Pattern search: grep -rn for code patterns

          ## FORBIDDEN
          ⛔ NEVER edit files (delegate to python-fixer)
          ⛔ NEVER validate (delegate to python-validator)
          ⛔ NEVER edit .yaml, .md, .json files

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am python-analyzer (v1.0.0) | MODE-CHECK: mas-engineer | 🔬 PYTHON-ANALYZER (v1.0.0)
          ⛔ ONLY Python code analysis — NO fixing or validation
          → Analyze Python files and return results
        settings:
          timeout: 300
          max_steps: 30
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted ANALYZE and COMPILE roles from python-repair.
        Single-responsibility: only code analysis. No fix or validate.
      priority: 0.58
      current_chars: 0
      target_chars: 950

    - type: create_sub_agent
      file: recipe/sub/sub_mas-python-fixer.yaml
      content: |
        name: MAS Python Fixer
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 🛠️ SUB-MAS-PYTHON-FIXER — Python Code Repair
        description: 'v1.0.0 | MAS-internal: Python code repair and patching'
        instructions: |
          # PYTHON-FIXER v1.0.0
          # Fix Python code — targeted edits and rewrites

          ## ⛔ ONLY fixing — NO analysis or validation
          → Fix Python files based on fix_description

          ## Procedure
          STEP 1: Backup file (cp or dev_editor.py task=BACKUP)
          STEP 2: Apply fix_description (edit/write)
          STEP 3: Ask user: "Should the fix be accepted?"
          STEP 4: On reject: restore from .bak

          ## FORBIDDEN
          ⛔ NEVER analyze (delegate to python-analyzer)
          ⛔ NEVER validate (delegate to python-validator)
          ⛔ NEVER edit .yaml, .md, .json files

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am python-fixer (v1.0.0) | MODE-CHECK: mas-engineer | 🛠️ PYTHON-FIXER (v1.0.0)
          ⛔ ONLY Python code repair — NO analysis or validation
          → Fix Python files based on fix_description
        settings:
          timeout: 300
          max_steps: 30
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted FIX role from python-repair.
        Single-responsibility: only code repair. No analysis or validate.
      priority: 0.58
      current_chars: 0
      target_chars: 850

    - type: create_sub_agent
      file: recipe/sub/sub_mas-python-validator.yaml
      content: |
        name: MAS Python Validator
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: ✅ SUB-MAS-PYTHON-VALIDATOR — Python Code Validation
        description: 'v1.0.0 | MAS-internal: Python code validation and verification'
        instructions: |
          # PYTHON-VALIDATOR v1.0.0
          # Validate Python code — full verification

          ## ⛔ ONLY validation — NO analysis or fixing
          → Validate Python files and return results

          ## Capabilities
          - Syntax check: python3 -c "compile(...)"
          - Print debugging detection: grep -n 'print('
          - Hardcoded path detection: grep -n '/home/'
          - Open TODO detection: grep -n 'TODO|FIXME|HACK'

          ## FORBIDDEN
          ⛔ NEVER analyze (delegate to python-analyzer)
          ⛔ NEVER fix (delegate to python-fixer)
          ⛔ NEVER edit .yaml, .md, .json files

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am python-validator (v1.0.0) | MODE-CHECK: mas-engineer | ✅ PYTHON-VALIDATOR (v1.0.0)
          ⛔ ONLY Python code validation — NO analysis or fixing
          → Validate Python files and return results
        settings:
          timeout: 300
          max_steps: 30
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted VALIDATE role from python-repair.
        Single-responsibility: only code validation. No analysis or fix.
      priority: 0.58
      current_chars: 0
      target_chars: 900

    - type: archive_original
      file: recipe/sub/legacy/sub_mas-python-repair-ORIGINAL.yaml
      from: recipe/sub/sub_mas-python-repair.yaml
      reason: |
        Why: Original python-repair archived after NN1 split into orchestrator + 3 sub-agents.
        Original file replaced with director recipe.
      priority: 0.58
      current_chars: 1450
      target_chars: 1450

    # ─────────────────────────────────────────────────────────────
    # F-1728: sub_mas-framework-scanner.yaml — NN1 split (6 roles)
    # Roles: validate, audit, scan, read, write
    # Tasks: SCAN, AUDIT, HARDEN_CHECK
    # ─────────────────────────────────────────────────────────────
    - type: create_orchestrator
      file: recipe/sub/sub_mas-framework-scanner-director.yaml
      content: |
        name: MAS Framework Scanner Director
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 🔍 SUB-MAS-FRAMEWORK-SCANNER-DIRECTOR — Framework Analysis Orchestrator
        description: 'v1.0.0 | MAS-internal: Orchestrates framework analysis by delegating to specialized sub-agents'
        instructions: |
          # FRAMEWORK-SCANNER-DIRECTOR v1.0.0
          # Orchestrator — delegates to specialized sub-agents

          ## ⛔ ONLY orchestration — NO direct framework analysis
          → Delegate to specialized sub-agents based on task

          ## Delegation Map
          - scan/overview → sub_mas-framework-scan-agent
          - audit/analyze → sub_mas-framework-audit-agent
          - harden/check → sub_mas-framework-harden-agent

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am framework-scanner-director (v1.0.0) | MODE-CHECK: mas-engineer | 🔍 FRAMEWORK-SCANNER-DIRECTOR (v1.0.0)
          ⛔ ONLY orchestration — NO direct framework analysis
          → Delegate to specialized sub-agents based on task
        sub_recipes:
          - name: sub_mas-framework-scan-agent
            path: ./sub_mas-framework-scan-agent.yaml
            description: Framework scanning and overview
          - name: sub_mas-framework-audit-agent
            path: ./sub_mas-framework-audit-agent.yaml
            description: Framework auditing and analysis
          - name: sub_mas-framework-harden-agent
            path: ./sub_mas-framework-harden-agent.yaml
            description: Framework hardening checks
        settings:
          timeout: 600
          max_steps: 100
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: multi_role_agent with 6 distinct roles (validate, audit, scan, read, write).
        Split into orchestrator + 3 specialized sub-agents (scan, audit, harden).
        Each sub-agent has single responsibility. Original archived to legacy.
        Goose-expert verdict: NOT REQUIRED — NN1 type not in mandatory consultation list.
      priority: 0.58
      current_chars: 1100
      target_chars: 1650

    - type: create_sub_agent
      file: recipe/sub/sub_mas-framework-scan-agent.yaml
      content: |
        name: MAS Framework Scan Agent
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 📊 SUB-MAS-FRAMEWORK-SCAN-AGENT — Framework Scanning
        description: 'v1.0.0 | MAS-internal: Scans framework structure and file inventory'
        instructions: |
          # FRAMEWORK-SCAN-AGENT v1.0.0
          # Scan framework — file inventory and structure

          ## ⛔ ONLY scanning — NO auditing or hardening
          → Scan framework files and return inventory

          ## Procedure SCAN
          1. IF dev_observer.py exists:
             python3 {tools_dir}/dev_observer.py --workspace {workspace} --scan
             OTHERWISE (Fallback):
             find {workspace} -type f | wc -l
             find {workspace} -name "*.yaml" | wc -l
             find {workspace} -name "*.md" | wc -l
             find {workspace} -name "*.py" | wc -l
             du -sh {workspace}
          2. Extract results: Files, sizes, structure
          3. Return

          ## FORBIDDEN
          ⛔ NEVER audit (delegate to framework-audit-agent)
          ⛔ NEVER harden (delegate to framework-harden-agent)
          ⛔ NEVER edit files

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am framework-scan-agent (v1.0.0) | MODE-CHECK: mas-engineer | 📊 FRAMEWORK-SCAN-AGENT (v1.0.0)
          ⛔ ONLY framework scanning — NO auditing or hardening
          → Scan framework files and return inventory
        settings:
          timeout: 600
          max_steps: 100
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted SCAN role from framework-scanner.
        Single-responsibility: only file inventory and structure scanning.
      priority: 0.58
      current_chars: 0
      target_chars: 1050

    - type: create_sub_agent
      file: recipe/sub/sub_mas-framework-audit-agent.yaml
      content: |
        name: MAS Framework Audit Agent
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 📋 SUB-MAS-FRAMEWORK-AUDIT-AGENT — Framework Auditing
        description: 'v1.0.0 | MAS-internal: Audits framework structure and architecture'
        instructions: |
          # FRAMEWORK-AUDIT-AGENT v1.0.0
          # Audit framework — structure and architecture analysis

          ## ⛔ ONLY auditing — NO scanning or hardening
          → Audit framework and return analysis

          ## Procedure AUDIT
          1. IF dev_observer.py exists:
             python3 {tools_dir}/dev_observer.py --workspace {workspace} --save
             OTHERWISE (Fallback):
             find {workspace} -type f -exec ls -lh {} \; > /tmp/observer_fallback.txt
          2. IF dev_architect.py exists:
             python3 {tools_dir}/dev_architect.py --workspace {workspace} --analyze
             OTHERWISE (Fallback):
             tree {workspace} -L 2 > /tmp/architect_fallback.txt
          3. IF dev_analyst.py exists:
             python3 {tools_dir}/dev_analyst.py --workspace {workspace} --check-all
             OTHERWISE (Fallback):
             for f in $(find {workspace} -name "*.yaml"); do echo "=== $f ==="; cat "$f"; done > /tmp/analyst_fallback.txt
          4. Consolidate all results → a Report

          ## FORBIDDEN
          ⛔ NEVER scan (delegate to framework-scan-agent)
          ⛔ NEVER harden (delegate to framework-harden-agent)
          ⛔ NEVER edit files

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am framework-audit-agent (v1.0.0) | MODE-CHECK: mas-engineer | 📋 FRAMEWORK-AUDIT-AGENT (v1.0.0)
          ⛔ ONLY framework auditing — NO scanning or hardening
          → Audit framework and return analysis
        settings:
          timeout: 600
          max_steps: 100
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted AUDIT role from framework-scanner.
        Single-responsibility: only architecture and structure auditing.
      priority: 0.58
      current_chars: 0
      target_chars: 1200

    - type: create_sub_agent
      file: recipe/sub/sub_mas-framework-harden-agent.yaml
      content: |
        name: MAS Framework Harden Agent
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 🛡️ SUB-MAS-FRAMEWORK-HARDEN-AGENT — Framework Hardening
        description: 'v1.0.0 | MAS-internal: Checks framework hardening and security'
        instructions: |
          # FRAMEWORK-HARDEN-AGENT v1.0.0
          # Harden framework — security and quality checks

          ## ⛔ ONLY hardening checks — NO scanning or auditing
          → Check framework hardening and return results

          ## Procedure HARDEN_CHECK
          1. IF dev_analyst.py exists:
             python3 {tools_dir}/dev_analyst.py --workspace {workspace} --check-all
             OTHERWISE (Fallback):
             find {workspace} -name "*.yaml" -exec sh -c 'echo "=== {} ==="; python3 -c "import yaml; yaml.safe_load(open(\"{}\"))" 2>&1 || echo "YAML-Error in {}"' \;
          2. Categorize problems by severity:
             🔴 CRITICAL: YAML-Error, broken Tests
             🟠 MEDIUM: Inconsistencies, Warnings
             🟢 LOW: Improvement suggestions
          3. Return list with suggestions

          ## FORBIDDEN
          ⛔ NEVER scan (delegate to framework-scan-agent)
          ⛔ NEVER audit (delegate to framework-audit-agent)
          ⛔ NEVER edit files

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          I am framework-harden-agent (v1.0.0) | MODE-CHECK: mas-engineer | 🛡️ FRAMEWORK-HARDEN-AGENT (v1.0.0)
          ⛔ ONLY framework hardening checks — NO scanning or auditing
          → Check framework hardening and return results
        settings:
          timeout: 600
          max_steps: 100
          goose_provider: openai
          goose_model: deepseek-chat
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted HARDEN_CHECK role from framework-scanner.
        Single-responsibility: only hardening and security checks.
      priority: 0.58
      current_chars: 0
      target_chars: 1100

    - type: archive_original
      file: recipe/sub/legacy/sub_mas-framework-scanner-ORIGINAL.yaml
      from: recipe/sub/sub_mas-framework-scanner.yaml
      reason: |
        Why: Original framework-scanner archived after NN1 split into orchestrator + 3 sub-agents.
        Original file replaced with director recipe.
      priority: 0.58
      current_chars: 1100
      target_chars: 1100

    # ─────────────────────────────────────────────────────────────
    # F-1729: sub_mas-test-fix-failures-validator.yaml — NN1 split (5 roles)
    # Roles: validate, report, design, read, test
    # ─────────────────────────────────────────────────────────────
    - type: create_orchestrator
      file: recipe/sub/sub_mas-test-fix-failures-validator-director.yaml
      content: |
        name: test-fix-failures-validator-director
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: ✓ SUB-MAS-TEST-FIX-FAILURES-VALIDATOR-DIRECTOR — Validation Orchestrator
        description: 'v1.0.0 | MAS-internal: Orchestrates patch validation by delegating to specialized sub-agents'
        instructions: |
          # TEST-FIX-FAILURES-VALIDATOR-DIRECTOR v1.0.0
          # Orchestrator — delegates to specialized sub-agents

          ## ⛔ ONLY orchestration — NO direct validation
          → Delegate to specialized sub-agents based on validation domain

          ## Delegation Map
          - syntax/yaml → sub_mas-tff-syntax-validator
          - rules/compliance → sub_mas-tff-rule-validator
          - crossref/consistency → sub_mas-tff-crossref-validator

          ## Process
          1. Read patches from designer
          2. Delegate to sub-agents for each validation domain
          3. Collect verdicts
          4. Return consolidated result: APPROVED or REJECTED

          ## RULES
          - R01: No changes without User confirmation
          - R09: Strict domain separation
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          ⛔ I am test-fix-failures-validator-director (v1.0.0) | MODE-CHECK: mas-engineer | ✓ TFF-VALIDATOR-DIRECTOR (v1.0.0)
          ⛔ ONLY orchestration — NO direct validation
          → Delegate to specialized sub-agents based on validation domain
        sub_recipes:
          - name: sub_mas-tff-syntax-validator
            path: ./sub_mas-tff-syntax-validator.yaml
            description: YAML syntax validation
          - name: sub_mas-tff-rule-validator
            path: ./sub_mas-tff-rule-validator.yaml
            description: Goose rule compliance validation
          - name: sub_mas-tff-crossref-validator
            path: ./sub_mas-tff-crossref-validator.yaml
            description: Cross-reference consistency validation
        settings:
          timeout: 300
          max_steps: 50
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: multi_role_agent with 5 distinct roles (validate, report, design, read, test).
        Split into orchestrator + 3 specialized sub-agents (syntax, rule, crossref).
        Each sub-agent has single responsibility. Original archived to legacy.
        Goose-expert verdict: NOT REQUIRED — NN1 type not in mandatory consultation list.
      priority: 0.58
      current_chars: 1200
      target_chars: 1550

    - type: create_sub_agent
      file: recipe/sub/sub_mas-tff-syntax-validator.yaml
      content: |
        name: tff-syntax-validator
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 📐 SUB-MAS-TFF-SYNTAX-VALIDATOR — YAML Syntax Validation
        description: 'v1.0.0 | MAS-internal: Validates YAML syntax of patches'
        instructions: |
          # TFF-SYNTAX-VALIDATOR v1.0.0
          # Validate YAML syntax of patches

          ## ⛔ ONLY YAML syntax validation — NO rule or crossref checks
          → Validate YAML syntax and return verdict

          ## Validation
          1. Parse each patch YAML with yaml.safe_load
          2. Check for syntax errors
          3. Return: PASS or FAIL with error details

          ## FORBIDDEN
          ⛔ NEVER check rules (delegate to tff-rule-validator)
          ⛔ NEVER check crossrefs (delegate to tff-crossref-validator)
          ⛔ NEVER edit files

          ## RULES
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          ⛔ I am tff-syntax-validator (v1.0.0) | MODE-CHECK: mas-engineer | 📐 TFF-SYNTAX-VALIDATOR (v1.0.0)
          ⛔ ONLY YAML syntax validation — NO rule or crossref checks
          → Validate YAML syntax and return verdict
        settings:
          timeout: 120
          max_steps: 20
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted YAML syntax validation role from test-fix-failures-validator.
        Single-responsibility: only YAML syntax checking.
      priority: 0.58
      current_chars: 0
      target_chars: 650

    - type: create_sub_agent
      file: recipe/sub/sub_mas-tff-rule-validator.yaml
      content: |
        name: tff-rule-validator
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 📜 SUB-MAS-TFF-RULE-VALIDATOR — Goose Rule Compliance
        description: 'v1.0.0 | MAS-internal: Validates patches against Goose rules (R01-R18)'
        instructions: |
          # TFF-RULE-VALIDATOR v1.0.0
          # Validate Goose rule compliance of patches

          ## ⛔ ONLY rule compliance — NO syntax or crossref checks
          → Validate Goose rule compliance and return verdict

          ## Validation Criteria
          1. Goose rule compliance (R01-R18)
          2. No duplicate responsibilities
          3. Proper summon extension declarations

          ## FORBIDDEN
          ⛔ NEVER check syntax (delegate to tff-syntax-validator)
          ⛔ NEVER check crossrefs (delegate to tff-crossref-validator)
          ⛔ NEVER edit files

          ## RULES
          - R01-R18: Full rule compliance check
        prompt: |
          ⛔ I am tff-rule-validator (v1.0.0) | MODE-CHECK: mas-engineer | 📜 TFF-RULE-VALIDATOR (v1.0.0)
          ⛔ ONLY Goose rule compliance — NO syntax or crossref checks
          → Validate Goose rule compliance and return verdict
        settings:
          timeout: 120
          max_steps: 20
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted rule compliance validation role from test-fix-failures-validator.
        Single-responsibility: only Goose rule compliance checking.
      priority: 0.58
      current_chars: 0
      target_chars: 700

    - type: create_sub_agent
      file: recipe/sub/sub_mas-tff-crossref-validator.yaml
      content: |
        name: tff-crossref-validator
        version: 1.0.0
        constitution: sub_mas-master-constitution.yaml
        title: 🔗 SUB-MAS-TFF-CROSSREF-VALIDATOR — Cross-Reference Consistency
        description: 'v1.0.0 | MAS-internal: Validates cross-reference consistency of patches'
        instructions: |
          # TFF-CROSSREF-VALIDATOR v1.0.0
          # Validate cross-reference consistency of patches

          ## ⛔ ONLY crossref consistency — NO syntax or rule checks
          → Validate cross-reference consistency and return verdict

          ## Validation
          1. Check that all referenced files exist
          2. Verify sub_recipes paths are valid
          3. Check extension references are consistent
          4. Return: PASS or FAIL with details

          ## FORBIDDEN
          ⛔ NEVER check syntax (delegate to tff-syntax-validator)
          ⛔ NEVER check rules (delegate to tff-rule-validator)
          ⛔ NEVER edit files

          ## RULES
          - R10: CORONASHIELD — validate YAML before storage
        prompt: |
          ⛔ I am tff-crossref-validator (v1.0.0) | MODE-CHECK: mas-engineer | 🔗 TFF-CROSSREF-VALIDATOR (v1.0.0)
          ⛔ ONLY cross-reference consistency — NO syntax or rule checks
          → Validate cross-reference consistency and return verdict
        settings:
          timeout: 120
          max_steps: 20
          temperature: 0.3
        extensions:
        - type: platform
          name: summon
        - type: builtin
          name: developer
          display_name: Developer
          timeout: 300
          bundled: true
      reason: |
        Why: Extracted cross-reference validation role from test-fix-failures-validator.
        Single-responsibility: only cross-reference consistency checking.
      priority: 0.58
      current_chars: 0
      target_chars: 700

    - type: archive_original
      file: recipe/sub/legacy/sub_mas-test-fix-failures-validator-ORIGINAL.yaml
      from: recipe/sub/sub_mas-test-fix-failures-validator.yaml
      reason: |
        Why: Original test-fix-failures-validator archived after NN1 split into orchestrator + 3 sub-agents.
        Original file replaced with director recipe.
      priority: 0.58
      current_chars: 1200
      target_chars: 1200

    # ─────────────────────────────────────────────────────────────
    # F-1730: sub_mas-test-director.yaml — NN1 split (7 roles)
    # Roles: analyze, validate, monitor, report, scan
    # Already has orchestrator structure but prompt/instructions list multiple roles
    # ─────────────────────────────────────────────────────────────
    - type: modify_prompt
      file: recipe/sub/sub_mas-test-director.yaml
      field: prompt
      from: |
        I am test-director (v1.0.0) | MODE-CHECK: mas-engineer | 🧪 TEST-DIRECTOR (v1.0.0)
        ⛔ ONLY orchestration — NO direct test execution
        → Delegate to specialized sub-agents based on test domain
      to: |
        I am test-director (v1.0.0) | MODE-CHECK: mas-engineer | 🧪 TEST-DIRECTOR (v1.0.0)
        ⛔ ONLY orchestration — NO direct test execution
        → Delegate to specialized sub-agents based on test domain
        ⛔ NEVER analyze, validate, monitor, report, or scan directly
        → Use sub_recipes for all domain operations
      reason: |
        Why: test-director already has orchestrator structure with sub_recipes and delegation map,
        but prompt lists 7 roles (analyze, validate, monitor, report, scan) creating ambiguity.
        Added explicit ⛔ prohibitions to enforce single-responsibility (orchestration only).
        Sub-agents (test-agent, test-reporter, test-scanner) already exist with single roles.
        Goose-expert verdict: NOT REQUIRED — NN1 type not in mandatory consultation list.
      priority: 0.58
      current_chars: 210
      target_chars: 310

    - type: modify_instructions
      file: recipe/sub/sub_mas-test-director.yaml
      field: instructions
      from: |
        # TEST-DIRECTOR v1.0.0
        # Orchestrator — delegates to specialized sub-agents

        ## ⛔ ONLY orchestration — NO direct test execution
        → Delegate to specialized sub-agents based on test domain

        ## Delegation Map
        - run/execute/verify → sub_mas-test-agent
        - analyze/report/document → sub_mas-test-reporter
        - scan/monitor/find → sub_mas-test-scanner

        ## RULES
        - R01: No changes without User confirmation
        - R09: Strict domain separation
        - R10: CORONASHIELD — validate YAML before storage
      to: |
        # TEST-DIRECTOR v1.0.0
        # Orchestrator — delegates to specialized sub-agents

        ## ⛔ ONLY orchestration — NO direct test execution
        → Delegate to specialized sub-agents based on test domain

        ## ⛔ PROHIBITIONS
        ⛔ NEVER execute tests directly (delegate to sub_mas-test-agent)
        ⛔ NEVER analyze or report (delegate to sub_mas-test-reporter)
        ⛔ NEVER scan or monitor (delegate to sub_mas-test-scanner)

        ## Delegation Map
        - run/execute/verify → sub_mas-test-agent
        - analyze/report/document → sub_mas-test-reporter
        - scan/monitor/find → sub_mas-test-scanner

        ## RULES
        - R01: No changes without User confirmation
        - R09: Strict domain separation
        - R10: CORONASHIELD — validate YAML before storage
      reason: |
        Why: Added explicit ⛔ PROHIBITIONS section to enforce single-responsibility.
        The delegation map was already correct but lacked prohibitions against direct action.
        This prevents the orchestrator from bypassing its sub-agents.
      priority: 0.58
      current_chars: 420
      target_chars: 560

  skipped:
    - finding_id: F-1726
      type: E1
      file: recipe/sub/sub_mas-intention-parser.yaml
      reason: "Skipped per STEP 0 MODE-CHECK: mode=framework, skip_mas_patches=True. E-patches (intention-parser patterns) are in the skip list for non-MAS modes."
      why_skipped: "mode != mas → skip D-, E- and MM-Constitution-Patches"

  scores:
    high: 0
    medium: 52
    low: 1725
