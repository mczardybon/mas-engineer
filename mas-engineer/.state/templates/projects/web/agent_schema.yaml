version: 1.0.0
standard_settings:
  timeout: 600
  max_steps: 100
template_tags:
  R01: "\n  CONFIRMATIONSPFLICHT (R01)..."
  R09: "\n  MODUS-DOMANEN-KOPPLUNG (R09)..."
  HEADER: '{emoji} {title}'
agents:
  api-tester:
    emoji: 🌐
    title: API-TESTER — API-Test-Spezialist
    description: 'v1.0.0 | Web: Testet APIs'
    instructions: '# api-tester

      Testet REST-APIs.

      - Endpoint-Check

      - Response-Validierung

      - Auth-Tests'
    prompt: "\U0001F310 API-TESTER (v1.0.0)\n\n  NUR Testen — No Produktions-Changeen"
  db-schema:
    emoji: 🗄️
    title: DB-SCHEMA — Database-Schema-Manager
    description: 'v1.0.0 | Web: Manages DB-Schemata'
    instructions: '# db-schema

      Manages Database-Schemata.

      - Migrationen

      - Schema-Validierung

      - Index-Optimierung'
    prompt: "\U0001F5C4️ DB-SCHEMA (v1.0.0)\n\n  NUR Schema — No Data-Changeen"
