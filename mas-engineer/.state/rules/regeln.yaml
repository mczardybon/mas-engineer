haerte_leveln:
  extrem_stark:
    beschreibung: Will NIE deleted. Blocked bei Violation.
    farbe: 🔴
    level: 5
    symbol: ⛔⛔⛔⛔⛔
    token_reserve: 800
  normal:
    beschreibung: Can verschwinden if Context voll.
    farbe: 🟡
    level: 2
    symbol: ⛔
    token_reserve: 300
  schwach:
    beschreibung: Optional. Will only bei freiem Context loaded.
    farbe: ⚪
    level: 1
    symbol: ⚠️
    token_reserve: 100
  stark:
    beschreibung: Bleibt bis Turn-End. No Block, aber Warning.
    farbe: 🟠
    level: 4
    symbol: ⛔⛔⛔
    token_reserve: 500
last_updated: '2026-06-14T22:00:00'
rulen:
- block: true
  check: python3 tools/dev_rule_checker.py --check R09 --action {aktion}
  haerte: 5
  id: R09
  name: MODUS_DOMAENEN_KOPPLUNG
  prompt_text: MODUS-DOMAENEN-KOPPLUNG — Modus determines Domain. MAS-NUR mas-engineer/. Framework-NUR framework/. Generic-NUR eigene Domain. Importe verboten. Read in andere Domain OK.
- block: true
  check: python3 tools/dev_rule_checker.py --check R01 --action {aktion}
  haerte: 5
  id: R01
  name: CONFIRMATIONSPFLICHT
  prompt_text: CONFIRMATION vor write/edit — Plan show + WARTEN auf ✅
- block: true
  check: null
  haerte: 5
  id: R02
  name: BESTAND_PRUFUNG
  prompt_text: INVENTORY vor NEUBAU — exists already? User ask
- block: true
  check: python3 tools/dev_rule_checker.py --check R04 --action {aktion}
  haerte: 5
  id: R04
  name: GENERAL_IMPROVER_SCHUTZ
  prompt_text: NEVER general-improver.yaml edit (no Rekursion)
- block: true
  check: null
  haerte: 5
  id: R05
  name: AUTO_COMMIT
  prompt_text: AUTO-COMMIT after Change — git+checkpoint+changes.json+todo
- block: false
  check: null
  haerte: 4
  id: R06
  name: SUB_AGENT_CONTAINMENT
  prompt_text: Sub-Agent = NUR Analyse — Shell selbst ausexecuten
- block: false
  check: null
  haerte: 2
  id: R07
  name: SIGNAL_CP_DONE
  prompt_text: CP_DONE after Checkpoint
- block: false
  check: null
  haerte: 2
  id: R08
  name: TOKEN_BUDGET
  prompt_text: Self-Improver max 50K — else User ask
- block: true
  check: python3 tools/dev_rule_checker.py --check R10 --action {aktion}
  haerte: 5
  id: R10
  name: CORONASHIELD
  prompt_text: CORONASHIELD — JEDE YAML will vor Speicherung via sub_mas-recovery-immune CHECK_YAML validated
version: 1.0.0
