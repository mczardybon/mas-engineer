haerte: 5
rulen:
- block: true
  check: python3 tools/dev_rule_checker.py --check R01 --action {aktion}
  haerte: 5
  id: R01
  name: CONFIRMATIONSPFLICHT
  prompt_text: CONFIRMATION vor write/edit — Plan show + WARTEN auf ✅
  text: ⛔⛔⛔⛔⛔ CONFIRMATION vor write/edit — Plan show + WARTEN auf ✅
- block: true
  check: null
  haerte: 5
  id: R02
  name: BESTAND_PRUFUNG
  prompt_text: INVENTORY vor NEUBAU — exists already? User ask
  text: ⛔⛔⛔⛔⛔ INVENTORY vor NEUBAU — exists already? User ask
- block: true
  check: python3 tools/dev_rule_checker.py --check R04 --action {aktion}
  haerte: 5
  id: R04
  name: GENERAL_IMPROVER_SCHUTZ
  prompt_text: NEVER general-improver.yaml edit (no Rekursion)
  text: ⛔⛔⛔⛔⛔ NEVER general-improver.yaml edit
- block: true
  check: python3 tools/dev_rule_checker.py --check R09 --action {aktion}
  haerte: 5
  id: R09
  name: MODUS_DOMAENEN_KOPPLUNG
  prompt_text: MODUS-DOMAENEN-KOPPLUNG — Modus determines Domain. Importe verboten.
  text: ⛔⛔⛔⛔⛔ MODUS-DOMAENEN-KOPPLUNG — Modus determines Domain
- block: true
  check: python3 tools/dev_rule_checker.py --check R05 --action {aktion}
  haerte: 5
  id: R05
  name: AUTO_COMMIT
  prompt_text: AUTO-COMMIT after write/edit — git commit + changes.json + checkpoint
  text: ⛔⛔⛔⛔⛔ AUTO-COMMIT after Change — git+checkpoint+changes
- block: false
  check: python3 tools/dev_rule_checker.py --check R06 --action {aktion}
  haerte: 4
  id: R06
  name: SUB_AGENT_CONTAINMENT
  prompt_text: Sub-Agent delegate = NUR Analyse. Shell selbst execute.
  text: ⛔⛔⛔ SUB-AGENT-CONTAINMENT — Sub-Agent = NUR Analyse
- block: false
  check: python3 tools/dev_rule_checker.py --check R07 --action {aktion}
  haerte: 2
  id: R07
  name: SIGNAL_CP_DONE
  prompt_text: CP_DONE after Checkpoint — else WARNINGUNG
  text: ⛔ SIGNAL_CP_DONE after Checkpoint
- block: false
  check: python3 tools/dev_rule_checker.py --check R08 --action {aktion}
  haerte: 2
  id: R08
  name: TOKEN_BUDGET
  prompt_text: SI-RUN max 50K Tokens — else User ask
  text: ⛔ TOKEN-BUDGET — Self-Improver max 50K
- block: true
  check: python3 tools/dev_rule_checker.py --check R10 --action {aktion}
  haerte: 5
  id: R10
  name: CORONASHIELD
  prompt_text: CORONASHIELD — JEDE YAML will vor Speicherung via sub_mas-recovery-immune CHECK_YAML validated
