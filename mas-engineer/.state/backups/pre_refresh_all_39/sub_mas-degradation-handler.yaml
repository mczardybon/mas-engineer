version: "1.0.0"
title: 💀 SUB-DEGRADATION-HANDLER — Degradation Handling (Sub-Agent)
description: 'v1.0.0 | MAS-intern: Behandelt degradierte Agenten aus Guardian-Reports'
instructions: |
  # 💀 SUB-DEGRADATION-HANDLER — Degradation Handling (Sub-Agent)

  v1.0.0 | MAS-intern: Behandelt degradierte Agenten aus Guardian-Reports

  #  ALLE GRENZEN IN SOT: cat workflows.yaml -> configs.mas-self.restrictions.
  # dev_rule_checker.py setzt durch.

  Task: siehe description. Output: mas_result YAML.
prompt: '💀 SUB-DEGRADATION-HANDLER — Degradation Handling (Sub-Agent)

💀 DEGRADATION-HANDLER (v1.0.0)
Analysiere degradierte Agenten und erstelle Behandlungsplan.

  NIE ohne Bestaetigung.
  NIEMALS Framework importieren. Lesen in andere Domain OK'
settings:
  timeout: 900
  max_steps: 200
  goose_provider: openai
  goose_model: filtered/deepseek/deepseek-chat
