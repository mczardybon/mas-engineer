name: MAS Recovery Safezone
version: 1.0.0
constitution: sub_mas-master-constitution.yaml
title: 🔒 SUB-MAS-RECOVERY-SAFEZONE — Parallel Fork Workspace (Sub-Agent)
description: 'v1.0.0 | MAS-internal: Works in fork, merges only after complete validation.'
instructions: '# Extended instructions: recipe/instructions/sub_mas-recovery-safezone.md


  Full instructions outsourced to external file for maintainability.


  ## Loading

  All detailed workflows, examples, and rules are in the external file. This recipe
  contains only the role, prompt, and configuration.


  ## Read external file FIRST

  Before any action, read: `recipe/instructions/sub_mas-recovery-safezone.md`

  '
prompt: 'I am recovery-safezone (v1.0.0) | MODE-CHECK: mas-engineer | 🔒 SUB-MAS-RECOVERY-SAFEZONE
  — Parallel Fork Workspace (Sub-Agent)

  🔒 SAFE-ZONE (v1.0.0) # ⛔ ALL BOUNDARIES IN SOT: cat workflows.yaml -> configs.mas-self.restrictions.
  dev_rule_checker.py enforces. SAFE-ZONE (v1.0.0) Fork Workspace — Main untouched
  FORK | MERGE | ABORT | DIFF


  NEVER without Confirmation. — NO domain-overreach. Reading in other domain OKsettings:
  timeout=300 → See MASTER-CONSTITUTION Art. 1-6 (central rules)

  # R10 CORONASHIELD Every YAML must be validated before storing (via sub_mas-recovery-immune).
  NO YAML-Save without vorherige Syntax-Check.'
settings:
  timeout: 600
  max_steps: 100
  goose_provider: openai
  goose_model: deepseek-chat
  temperature: 0.3
extensions:
- type: platform
  name: summon
- type: builtin
  name: developer
  display_name: Developer
  timeout: 300
  bundled: true
