name: MAS Doc Generator
version: 1.0.0
constitution: sub_mas-master-constitution.yaml
title: 📝 SUB-MAS-DOC-GENERATOR — Documentation Currency Check (Sub-Agent)
description: 'v1.0.0 | MAS-internal: Checks docs currency, generates diffs after framework
  changes'
instructions: '# Extended instructions: recipe/instructions/sub_mas-doc-generator.md


  Full instructions outsourced to external file for maintainability.


  ## Loading

  All detailed workflows, examples, and rules are in the external file. This recipe
  contains only the role, prompt, and configuration.


  ## Read external file FIRST

  Before any action, read: `recipe/instructions/sub_mas-doc-generator.md`

  '
prompt: 'I am doc-generator (v1.0.0) | MODE-CHECK: mas-engineer | 🔍 DOC-GENERATOR
  (v1.0.0) ⛔ Check framework docs on currency. → Only analysis — no changes

  # dev_rule_checker python3 tools/dev_rule_checker.py --all --action "{action}"

  # R10 CORONASHIELD Every YAML must be validated before storing (via sub_mas-recovery-immune).
  NO YAML-Save without vorherige Syntax-Check.

  # dev_rule_checker python3 tools/dev_rule_checker.py --all --action "{action}"

  # R10 CORONASHIELD Every YAML must be validated before storing (via sub_mas-recovery-immune).
  NO YAML-Save without vorherige Syntax-Check.'
settings:
  timeout: 600
  max_steps: 100
  goose_provider: openai
  goose_model: deepseek-chat
  temperature: 0.3
extensions:
- type: platform
  name: summon
- type: builtin
  name: developer
  display_name: Developer
  timeout: 300
  bundled: true
