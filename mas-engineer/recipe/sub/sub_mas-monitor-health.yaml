name: MAS Monitor Health
version: 2.0.0  # R85 Phase 2: replaced inline logic with tools/dev_health_monitor.py
constitution: sub_mas-master-constitution.yaml
title: 💚 FW-MONITOR-HEALTH — Framework Health Check (Script-wrapper)
description: 'v2.0.0 | CONTROLLER-internal: YAML-Integrity, Invariants, Governance, Structure. Delegates to tools/dev_health_monitor.py CHECK_HEALTH.'
instructions: |
  # MONITOR-HEALTH v2.0.0 (Script-wrapper)

  R85 Phase 2 REFACTOR: All logic moved to `tools/dev_health_monitor.py`.
  This recipe is a thin wrapper — it delegates health-checks to the script.

  ## Usage
  Call `tools/dev_health_monitor.py CHECK_HEALTH [recipes_dir]`.
  Returns JSON: checks_total/passed, findings[], structure counts, signal.

  ## SOT RULES
  ⛔ R01 CONFIRMATION — Before write/edit/shell PLAN+WAIT on user ✅.
  ⛔ R10 CORONASHIELD — YAML-validate each sample before storage.
prompt: |
  I am monitor-health (v2.0.0) | MODE-CHECK: mas-engineer | 💚 FW-MONITOR-HEALTH
  ⛔ ONLY Static-File-Analysis — NO changes. Use tools/dev_health_monitor.py CHECK_HEALTH.
settings:
  timeout: 180
  max_turns: 25
  goose_provider: openai
  goose_model: deepseek-v4-flash
  temperature: 0.3
extensions:
- type: platform
  name: summon
- type: builtin
  name: developer
  display_name: Developer
  timeout: 60
  bundled: true
