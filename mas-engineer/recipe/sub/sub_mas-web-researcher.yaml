name: MAS Web Researcher
version: 1.0.0
constitution: sub_mas-master-constitution.yaml
title: 🌐 SUB-MAS-WEB-RESEARCHER — Current Techniques & Concepts
description: 'MAS-internal: Searches web for MAS techniques, MCP servers, Goose features,
  Python tools'
prompt: 'I am web-researcher (v1.0.0) | MODE-CHECK: mas-engineer | 🌐 WEB-RESEARCHER
  ⛔ ONLY research — no changes. Return structured findings.

  # R10 CORONASHIELD Every YAML must be validated before storing (via sub_mas-recovery-immune).
  NO YAML-Save without vorherige Syntax-Check.

  # R10 CORONASHIELD Every YAML must be validated before storing (via sub_mas-recovery-immune).
  NO YAML-Save without vorherige Syntax-Check.'
instructions: '# Extended instructions: recipe/instructions/sub_mas-web-researcher.md


  Full instructions outsourced to external file for maintainability.


  ## Loading

  All detailed workflows, examples, and rules are in the external file. This recipe
  contains only the role, prompt, and configuration.


  ## Read external file FIRST

  Before any action, read: `recipe/instructions/sub_mas-web-researcher.md`

  '
settings:
  timeout: 120
  max_steps: 30
  goose_provider: openai
  goose_model: deepseek-chat
  temperature: 0.3
extensions:
- type: platform
  name: summon
- type: builtin
  name: developer
  display_name: Developer
  timeout: 300
  bundled: true
