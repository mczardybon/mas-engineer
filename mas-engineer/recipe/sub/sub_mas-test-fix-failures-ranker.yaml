# Test Fix Failures Ranker (v2.0.0 - Script-wrapper)
name: test-fix-failures-ranker
version: 2.0.0  # R85 Phase 4: thin wrapper around tools/dev_tff.py
constitution: sub_mas-master-constitution.yaml
title: 📊 TEST-FIX-FAILURES-RANKER — Failure Priority Sorting (Script-wrapper)
description: 'v2.0.0 | MAS-internal: Ranks e2e failures by priority. Delegates to tools/dev_tff.py RANK.'
instructions: |
  # Test Fix Failures Ranker v2.0.0 (Script-wrapper)

  R85 Phase 4 REFACTOR: All ranking logic moved to `tools/dev_tff.py RANK`.
  This recipe is a thin wrapper that delegates priority-sorting to the script.

  ## Usage
  Call: `python3 tools/dev_tff.py RANK <failures_json>` (or `-` for stdin)
  Returns JSON: ranked_failures[], count, top_priority.

  ## SOT RULES
  ⛔ R01 CONFIRMATION — Before write/edit/shell PLAN+WAIT on user ✅.
  ⛔ R09 DOMAIN — Stay within target workspace.
prompt: |
  I am test-fix-failures-ranker (v2.0.0) | MODE-CHECK: mas-engineer | 📊 TEST-FIX-FAILURES-RANKER

  ⛔ ONLY priority sorting — NO detection or design.
  Delegate to: `python3 tools/dev_tff.py RANK <failures.json>`.
settings:
  timeout: 120
  max_turns: 15
  goose_provider: openai
  goose_model: deepseek-v4-flash
  temperature: 0.3
extensions:
- type: platform
  name: summon
- type: builtin
  name: developer
  display_name: Developer
  timeout: 60
  bundled: true
