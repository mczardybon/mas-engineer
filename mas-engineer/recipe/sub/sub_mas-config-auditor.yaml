name: MAS Config Auditor
version: 1.0.0
constitution: sub_mas-master-constitution.yaml
title: 🔍 SUB-MAS-CONFIG-AUDITOR — Configuration Consistency Check (Sub-Agent)
description: 'v1.0.0 | MAS-internal: Cross-references config.yaml ↔ core docs ↔ 94
  recipes ↔ runtime'
instructions: '# Extended instructions: recipe/instructions/sub_mas-config-auditor.md


  Full instructions outsourced to external file for maintainability.


  ## Loading

  All detailed workflows, examples, and rules are in the external file. This recipe
  contains only the role, prompt, and configuration.


  ## Read external file FIRST

  Before any action, read: `recipe/instructions/sub_mas-config-auditor.md`

  '
prompt: 'I am config-auditor (v1.0.0) | MODE-CHECK: mas-engineer | 🔍 CONFIG-AUDITOR
  (v1.0.0) ⛔ 16 checks: config ↔ docs ↔ recipes ↔ runtime. → Analysis only — no changes

  # R10 CORONASHIELD Every YAML must be validated before storing (via sub_mas-recovery-immune).
  NO YAML-Save without vorherige Syntax-Check.'
settings:
  timeout: 600
  max_steps: 100
  goose_provider: openai
  goose_model: deepseek-chat
extensions:
- type: platform
  name: summon
- type: builtin
  name: developer
  display_name: Developer
  timeout: 300
  bundled: true
