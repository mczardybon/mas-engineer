# Recipe scaffolding file (R110-388 placeholder)
#
# Created: 2026-09-16 21:55 (replace empty 0-byte IDE auto-commit trap file)
# Purpose: prevent the IDE from auto-committing an empty tracked file
#          with subject `[]` (R110-388 pitfall). Real recipes use a
#          non-empty YAML body so git diff produces a valid diff and the
#          auto-commit trap does not trigger.
#
# This file is intentionally minimal — it's a placeholder folder/file so
# the directory exists for any user-level recipe scaffolding we add later.
# DO NOT delete this file unless the directory itself can be removed
# (i.e., all recipes under recipe/sub/ are gone).
