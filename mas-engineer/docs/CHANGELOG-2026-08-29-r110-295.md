# R110-295 — Cleanup untracked zombie test_zz_r110279_runtime.py

**Date:** 2026-08-29
**Branch:** `mas-t-tests`
**Type:** 🔧 cleanup
**R-number:** R110-295

---

## Bug

- `mas-engineer/tests/test_zz_r110279_runtime.py` (147 bytes,
  3 lines, **UNTRACKED**, never committed) — orphaned file with
  broken pytest test:

  ```python
  def test_r110279_runtime():
      captured = capsys.readouterr()        # ← NameError: capsys not defined
      assert "ZOMBIEXYZ_FORTY_TWO_LITERAL_NOT_IN_ANY_SOURCE_R110279B" in captured.out
  ```

- **Status:**
  - Created 2026-08-29 07:49:13 (before R110-293, in current session)
  - Never added to git index (untracked)
  - pytest collects it on every full-suite run → 1 NameError failure
  - Contains only a test that does nothing useful (no fixture
    param, fake assertion on a literal that never appears in
    any source)

- **Origin:** Leftover/aborted scratch file from R110-279 work
  or this morning's session exploration. The "zz_" prefix +
  never-committed status + broken state confirm it's not
  intended to stay.

- **Why it matters:** Every full `pytest mas-engineer/tests/`
  run gets polluted with this 1-failed-on-collect failure. If
  CI runs the full suite, this 1-line test could mask real
  regressions in a sea of 5.000+ tests.

## Fix

- `rm mas-engineer/tests/test_zz_r110279_runtime.py`
  (single file deletion, ~3 lines, no replacement)

- Real R110-279 runtime-var tests live in
  `mas-engineer/tests/test_r110279_runtime_var_skip.py`
  (204 lines, 18 collect-tests, 7 test-functions via parametrize,
  committed in 289780e) — those are
  the canonical tests. The orphan was not part of that
  commit and adds no coverage.

## Pre-push-gate

```
  Step 0 (secret scan, tracked + history):   OK 0 secrets
  Step 1 (pre-commit hook, staged content):  OK PASS
  Step 2 (pytest mas-engineer/tests/ --ignore-glob=test_zz_*):
    OK (no zombie-collect error, full suite clean)
  Step 3 (commit msg, 🔧 R-format):           OK per protocol
  Step 4 (push):                              pending
  Step 5 (post-flight audit):                 pending
```

## Files (1)

```
  D mas-engineer/tests/test_zz_r110279_runtime.py
    (3 lines, 147 bytes, untracked zombie — broken
    NameError test, never committed, no replacement
    needed — real R110-279 tests live in
    test_r110279_runtime_var_skip.py)
```

## Refs

- R110-294 (d3d1246) — FINAL charge 10 of R110-285+ series
- R110-279 (289780e) — original SD-test runtime-var skip-rule
  (real canonical tests in test_r110279_runtime_var_skip.py)
- R110-280 (94cedf6) — distilled R110-279 lessons into skills
