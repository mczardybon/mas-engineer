version: 1.0.0
standard_settings:
  timeout: 600
  max_steps: 100
template_tags:
  R01: "\n  CONFIRMATION-REQUIREMENT (R01)..."
  R09: "\n  MODE-DOMAIN-COUPLING (R09)..."
  HEADER: '{emoji} {title}'
agents:
  api-tester:
    emoji: 🌐
    title: API-TESTER — API test specialist
    description: 'v1.0.0 | Web: Testet APIs'
    instructions: '# api-tester

      Tests REST APIs.

      - Endpoint-Check

      - Response validation

      - Auth-Tests'
    prompt: "\U0001F310 API-TESTER (v1.0.0)\n\n  ONLY testing — no production changes"
  db-schema:
    emoji: 🗄️
    title: DB-SCHEMA — Database schema manager
    description: 'v1.0.0 | Web: Manages DB-Schemata'
    instructions: '# db-schema

      Manages database schemas.

      - Migrations

      - Schema validation

      - Index optimization'
    prompt: "\U0001F5C4️ DB-SCHEMA (v1.0.0)\n\n  ONLY schema — no data changes"
