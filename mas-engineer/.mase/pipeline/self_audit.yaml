audit_run:
  timestamp: "2026-08-19T04:50:36.134786+00:00"
  auditor: "sub_mas-self-audit (via dev_self_audit.py)"
  scope: "/workspace/dev-branch/mas-engineer-cleanup/mas-engineer/recipe/instructions"
  workspace: "/workspace/dev-branch/mas-engineer-cleanup/mas-engineer"
  files_scanned: 45
  findings_count: 20
  result: PASS
  summary: "0 blocker, 20 warn, 0 error (of 20 findings, 45 files)"
findings:
-
  id: "HARDCODE-CHECKS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-config-auditor.md:137: hardcoded '13 checks' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 13' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-CHECKS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-pre-push-validator.md:26: hardcoded '22 checks' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 22' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-bootstrap.md:4: hardcoded '112 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 112' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-bootstrap.md:16: hardcoded '112 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 112' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-bootstrap.md:40: hardcoded '112 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 112' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-config-auditor.md:93: hardcoded '43 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 43' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-config-auditor.md:98: hardcoded '43 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 43' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-generic-init.md:39: hardcoded '112 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 112' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-im-finder.md:549: hardcoded '47 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 47' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-pre-push-validator.md:531: hardcoded '112 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 112' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-system-knowledge.md:13: hardcoded '44 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 44' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-SUB-AGENTS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-team-packager.md:364: hardcoded '20 sub-agents' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 20' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-bootstrap.md:4: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-bootstrap.md:17: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-bootstrap.md:40: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-generic-init.md:39: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-system-knowledge.md:12: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-system-knowledge.md:133: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-system-knowledge.md:149: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
-
  id: "HARDCODE-TOOLS"
  severity: "WARN"
  description: "recipe/instructions/sub_mas-team-packager.md:65: hardcoded '58 tools' without env-var/default/config context"
  suggested_fix: "Reference an env var (e.g. IM_TOP_N), a documented 'default 58' value, or derive the count from the source of truth instead of hardcoding."
