# ============================================================
# SECRETS (R108-9 — defense in depth, 2026-07-27)
# .env enthaelt API keys + GitHub PAT. NIEMALS tracken.
# ============================================================
.env
.env.*
!.env.example
mas-engineer/.env
mas-engineer/.env.*
!mas-engineer/.env.example

# .keys — API-Key-Datei (DeepSeek etc.). NIEMALS tracken.
.keys
mas-engineer/.keys

# Vendor / external install-scripts (downloaded on-demand, not source)
# R110-102 (2026-08-04): goose-install.sh is the upstream Goose installer
# from https://github.com/block/goose — generic 14.7KB bootstrap, not a
# mas-engineer asset. Re-download via the curl command at the top of the
# file if needed. Tracked version would just rot.
goose-install.sh
mas-engineer/goose-install.sh

# actionlint binary (R110-246: 5MB Go binary installed locally for
# pre-push workflow schema validation. Downloaded from
# https://github.com/rhysd/actionlint/releases, not a source asset.
# Tracked version would just rot and bloat the repo.)
/actionlint

# ============================================================
# RUNTIME (regenerated, soll NICHT tracked)
# ============================================================

# Root-level .mase/ (R110-165 phase 1.1: legacy test writes + message-queue
# runtime — symmetric to mas-engineer/.mase/mq/ already ignored). Without
# this, root .mase/mq/test.ndjson etc. surface as untracked at the repo root.
.mase/

# Root-level e2e-results/ (R110-165 phase 1.1: runtime test output. Real
# evidence goes to logs/e2e-results/ which is intentionally committed per
# R110-143. The root e2e-results/ is just runtime noise.)
e2e-results/

# .mase/ runtime (NICHT: knowledge, rules, templates, SOT)
.mase/todo.md
.mase/changes.json
.mase/changes.unlimited-archive-*.json
.mase/health-history.json
.mase/health-report.json
.mase/audit.log.jsonl
.mase/audit_result.yaml
.mase/analysis.json
.mase/guardian.yaml
.mase/.last_confirmation
.mase/backups/
.mase/checkpoints/
.mase/workflow_runs/
.mase/dashboards/

# e2e-results/ runtime (R110-22 — raw-results.json ist mas-engineer
# workflow-run output, regeneriert sich bei jedem run, NICHT evidence)
# Echte e2e-evidence (z.B. EVIDENCE-*.md, EVIDENCE-*.json) wird explizit committed.
e2e-results/*/raw-results.json
# Auch unter logs/ — R110-151: top-level rule matched keine sub-pfade
# (warum die 2026-08-14 run-1..4 raw-results als untracked erschienen)
logs/e2e-results/*/raw-results.json

# ============================================================
# logs/ — SINGLE central destination for ALL mas-generated logs,
# evidence, test results and AI-generated reports (from mas-engineer
# or any other AI).
#
# The e2e logs/evidence under logs/e2e-results/, logs/e2e-evidence/ and
# logs/e2e-evidence-gen2/ are the PUBLIC PROOF that mas-engineer works.
# They ARE committed to GitHub (R110-143, 2026-08-07) so anyone can verify
# the claimed test results. They are scanned for secrets before commit
# (test_r110134_8_evidence_secret_leak) and must contain no API keys.
#
# Only NON-EVIDENCE artifacts stay ignored: build output (dist/), AI
# research outputs, and loose reports/scripts at the logs/ top level.
# ============================================================
logs/dist/
logs/outputs/
logs/seo-research-output/
logs/run_security_scanner.py
logs/run_static_analyzer.py
logs/recipe_live_test.yaml
logs/recipe_live_test_FINAL.yaml
logs/*.json
logs/*.py
logs/ai_agent_saas_marketing_report.md

# .mase/pipeline/ runtime (NICHT: findings_R*_structural, validation-R*.json)
.mase/pipeline/signals.log
.mase/pipeline/ranked_findings.yaml
.mase/pipeline/findings.yaml
.mase/pipeline/patches.yaml
.mase/pipeline/pre_push_validation.yaml
.mase/pipeline/summarizer_result.yaml
.mase/pipeline/summarizer_result_*.yaml
.mase/pipeline/signal_*.yaml
.mase/pipeline/round*_findings.json
.mase/pipeline/skip_recently_split.yaml
.mase/pipeline/self_audit.yaml
.mase/pipeline/README.md

# .mase/rules runtime
.mase/rules/.last_refresh
.mase/rules/.state

# .mase/ runtime
.mase/dashboards/data.json
.mase/dashboards/history.json
.mase/dashboards/.updated
.mase/live-daemon.log

# Worktrees
.worktrees/
.dev-team/.heartbeat

# Python / OS
__pycache__/
*.pyc
*.pyo
.DS_Store
*.swp
*.swo
.vscode/
.idea/
 *.log
 nohup.out
 # e2e evidence logs ARE committed (R110-143 — public proof mas works).
 # Re-include the .log files under logs/e2e-* that *.log above would ignore.
 !logs/e2e-results/**/*.log
 !logs/e2e-results/**/*.log.*
 !logs/e2e-evidence/**/*.log
 !logs/e2e-evidence/**/*.log.*
 !logs/e2e-evidence-gen2/**/*.log
 !logs/e2e-evidence-gen2/**/*.log.*
 !logs/e2e-results/**/_results.tsv
 !logs/e2e-results/**/raw-results.json

# Build / runtime
node_modules/
dist/
build/
*.egg-info/

# Temp
/tmp/
*.tmp
*.bak
*.backup
*.backup.*

# ============================================================
# ZUSÄTZLICH (P5+ cleanup 2026-07-25)
# ============================================================

# .backups/ — backup directories
.backups/
**/.backups/

# .dev-team/ — controller runtime
.dev-team/
**/.dev-team/

# .mas runtime extensions
.mase/dashboards/
**/.mase/dashboards/
.mase/live-daemon.pid
.mase/*.pid

# .state runtime extensions
.mase/coverage/
**/.mase/coverage/
.mase/schedule.yaml
**/.mase/schedule.yaml
.mase/pipeline/signal_apply_only_*.yaml
.mase/pipeline/im_apply_only_log.yaml
.mase/pipeline/self_audit*.yaml
.mase/pipeline/round*_findings.json
.mase/pipeline/skip_recently_split.yaml

# .bak files (specific timestamp pattern, e.g. *.bak.20260724_*)
*.bak.*
*.bak
**/*.bak.*

# testproject/ — test cruft (not production)
testproject/
mas-engineer/testproject/

# ============================================================
# MAS-ENGINEER SUBFOLDER SPECIFICS (R75+)
# ============================================================

mas-engineer/.mase/live-daemon.pid
mas-engineer/.mase/dashboards/
mas-engineer/.mase/pipeline/im_apply_only_log.yaml
mas-engineer/.mase/pipeline/signal_apply_only_*.yaml
mas-engineer/.mase/pipeline/round*_findings.json
mas-engineer/.mase/pipeline/skip_recently_split.yaml
mas-engineer/.mase/pipeline/self_audit*.yaml
mas-engineer/.mase/coverage/
mas-engineer/recipe/.backups/
mas-engineer/recipe/sub/.backups/
mas-engineer/testproject/

# ============================================================
# SOT-ORTE (R110-257, 2026-08-26): FALSCHE orte für
# directives + evidence-archive sind VERBOTEN.
#
# SOT:
#   - directives:        mas-engineer/.mase/directives/
#   - evidence-archive:  logs/e2e-evidence-gen2/  (REPO-ROOT)
#
# Per R110-143 (9ba8705, 2026-08-15) ist logs/e2e-evidence-gen2/ der
# zentrale evidence-ort (906 files committed damals). Per R110-193
# (bb36cd0, 2026-08-18) wurden files in mas-engineer/logs/e2e-evidence-gen2/
# als "verstoss gegen R110-143 precedent" markiert. Per dev_directive_applier.py
# (R110-115 DIREKTIVE 1) lesen alle tools directives aus
# mas-engineer/.mase/directives/ — der mas-engineer/.directives/ ordner
# existiert in der SOT nicht.
#
# Prevention layers:
#   1. .gitignore (dieser block) — files können nicht committed werden
#   2. pre-push-validator Check 19 (dev_evidence_sot.py) — fail-fast
#   3. dev_evidence_sot.py standalone — manueller SOT-ort-check
# ============================================================
# Block wrong SOT location for directives (committed accidentally
# in R110-217 + R110-218 — moved by R110-257 to .mase/directives/)
mas-engineer/.directives/
mas-engineer/.directives/**
# Block wrong SOT location for evidence-archive (26 files committed
# in R110-194/210/214/215/216/229/230/255 — moved by R110-257 to
# repo-root logs/e2e-evidence-gen2/)
mas-engineer/logs/
mas-engineer/logs/**

# ============================================================
# LEGACY SNAPSHOTS (history cruft, see tools/cleanup_repo_v1.sh P3)
# ============================================================
mas-engineer/recipe/sub/legacy/*-ORIGINAL.yaml
*-ORIGINAL.yaml
**/*-ORIGINAL.yaml

# Temp commit-msg files
COMMIT_EDITMSG_*.tmp
