MAS-ENGINEER E2E-FIX-VERIFICATION SUMMARY (honest scope)
=========================================================

Date: 2026-07-21
Issue: #7355/#7570 (goose 1.24+ sub_recipes dispatch failure)

VERIFIED (loading-level):
  - mas-engineer root recipe loads with type:platform
  - summon extension activates
  - 52 sub-recipes register in mas-engineer recipe
  - 3 demo-team root recipes load (sales/marketing/translator)
  - Pre-push validator: 8/8 PASS
  - Model with 52 sub-recipes as system prompts answers complex query
  - Tool calls (curl, find, gh) work in the flat-prompt setup

NOT VERIFIED (out of scope / requires TTY):
  - Interactive TUI dispatch (node.js TUI does not accept stdin from
    non-TTY sandbox)
  - 3 demo teams orchestrator actually delegating with --params
    (separate recipe-design issue: prompt: template does not read
    --params, orchestrator re-asks for same info)
  - The 52-sub-recipe "delegation" is a workaround (flat system
    prompts), NOT the orchestrator routing to a single specialist

TWO DISTINCT FAILURES:
  1. Recipe-load (type:builtin) — FIXED in d03efd2, 5431a40
  2. Orchestrator ignores --params — NOT FIXED, separate issue,
     out of scope. See RE-TEST-RESULTS.md.

WHAT A HUMAN USER MUST DO:
  - Open a real TTY, run goose run --recipe <path>
  - Type a query, observe orchestrator dispatching to specialists
  - If orchestrator re-asks for already-provided params, that is
    the second issue (not covered by this certificate)

The mas-engineer FRAMEWORK is functional.
The 3 demo teams need additional recipe-engineering work.
